version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Build source
          command: g++ -std=c++11 test.cpp
      - run:
          name: Run
          command: ./a.out

